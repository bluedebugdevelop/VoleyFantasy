import React from 'react';

/**
 * Pestaña "Inicio" del navbar de la liga: nunca se renderiza, su tabPress
 * está interceptado en el layout para volver al Home de la app.
 */
export default function InicioStub() {
  return null;
}
